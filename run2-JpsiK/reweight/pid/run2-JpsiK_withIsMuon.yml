trees:
    - tree

config:
    wpid_withIsMuon_mu:
        particle: Mu_nopt
        vars:
            - GUARD(mu_P, 100.0, 200000.0)
            - GUARD(ETA(mu_P, mu_PZ), 1.5, 5.5)
            - GUARD(nTracks, 0, 600)
    wpid_withIsMuon_amu:
        particle: Mu_nopt
        vars:
            - GUARD(amu_P, 100.0, 200000.0)
            - GUARD(ETA(amu_P, amu_PZ), 1.5, 5.5)
            - GUARD(nTracks, 0, 600)
    wpid_k:
        particle: K
        vars:
            - GUARD(k_P, 100.0, 200000.0)
            - GUARD(ETA(k_P, k_PZ), 1.5, 5.5)
            - GUARD(nTracks, 0, 600)
